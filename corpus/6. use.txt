==================
Use block
==================

use ($object)

end use

utiliser ($object)

fin utiliser

---

(source   (use_block     (use )    (arguments       (argument         (value           (reference             (variable               (local_variable ))))))    (end_use ))  (use_block     (use )    (arguments       (argument         (value           (reference             (variable               (local_variable ))))))    (end_use )))